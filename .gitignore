# NeoSpeech installer files are proprietary/copyrighted -- never commit them.
# Building this image requires supplying your own legally-obtained copy in a
# per-voice subdirectory (see README.md). Add new voice dirs here the same
# way if more get introduced.
/paul/
/violeta/

# build/debug artifacts
*.log
*.wav
build/

# local-only config (registry path, etc.)
.env
